# 📁 项目结构说明

## 🎯 设计原则

本项目采用**前后端分离**架构，遵循以下原则：

1. **清晰的目录边界**：前后端完全分离，各自独立
2. **分层忽略规则**：`.gitignore` 分为项目级和子目录级
3. **虚拟环境隔离**：Python 虚拟环境必须在 `backend/` 内
4. **临时文件集中**：所有临时文件放在 `.temp/` 目录

## 📂 当前目录结构

```
agentic_chat/
├── backend/                       # 🐍 后端目录（Python/FastAPI）
│   ├── app/                      # 应用代码
│   │   ├── api/                 # API 路由
│   │   ├── core/                # 核心模块
│   │   ├── db/                  # 数据库
│   │   ├── agents/              # Agent 实现
│   │   ├── services/            # 业务逻辑
│   │   └── main.py              # 入口文件
│   ├── data/                    # 数据库文件
│   ├── logs/                    # 后端日志
│   ├── tests/                   # 测试
│   ├── .venv/                   # ⚠️ Python 虚拟环境（必须在这里）
│   ├── requirements.txt         # Python 依赖
│   ├── .env                     # 后端配置
│   └── .gitignore               # 后端特定的 Git 忽略规则
│
├── frontend/                      # ⚛️ 前端目录（React/TypeScript）
│   ├── src/                     # 源代码
│   │   ├── components/         # React 组件
│   │   ├── api/                # API 调用
│   │   ├── store/              # 状态管理
│   │   ├── utils/              # 工具函数
│   │   └── App.tsx             # 主组件
│   ├── public/                  # 静态资源
│   ├── node_modules/            # Node 依赖
│   ├── package.json             # 前端依赖
│   ├── vite.config.ts           # Vite 配置
│   └── .gitignore               # 前端特定的 Git 忽略规则
│
├── docs/                          # 📚 文档
│   ├── deployment.md            # 部署指南
│   └── README.md                # 详细文档
│
├── .temp/                         # 🗑️ 临时文件（已忽略）
│   ├── README.md                # 说明
│   └── ...                      # 调试文档等
│
├── README.md                      # 项目主文档
├── QUICKSTART.md                  # 快速开始
├── PROJECT_STRUCTURE.md           # 📋 本文件（项目结构说明）
├── start-dev.sh                   # 启动脚本（Unix）
├── start-dev.bat                  # 启动脚本（Windows）
├── .gitignore                     # 项目级别的 Git 忽略规则
├── .cursorignore                  # Cursor 忽略配置
└── .cursorrules                   # Cursor AI 规则（记住项目结构）
```

## 🚫 忽略规则设计（重要！）

### 三层忽略规则架构

#### 1. 根目录 `.gitignore` - 项目级别
**作用**：处理项目级别的文件和目录

**包含内容**：
- `.temp/` - 临时文件夹
- `logs/` - 项目级别的日志
- `.env` - 项目级别的环境变量
- `*.pid` - 进程文件
- IDE 配置（`.vscode/`, `.idea/`）
- OS 文件（`.DS_Store`）

**不包含**：
- ❌ `.venv/` - 应该在 `backend/.gitignore`
- ❌ `node_modules/` - 应该在 `frontend/.gitignore`
- ❌ `__pycache__/` - 应该在 `backend/.gitignore`

#### 2. `backend/.gitignore` - Python 后端特定
**作用**：处理 Python 相关的文件和目录

**包含内容**：
- `.venv/`, `venv/` - **虚拟环境必须在这里**
- `__pycache__/`, `*.pyc` - Python 缓存
- `*.db`, `*.sqlite` - 数据库文件
- `backend/logs/` - 后端日志
- `backend/data/` - 后端数据
- `*.egg-info/` - Python 包信息
- `.pytest_cache/` - 测试缓存

#### 3. `frontend/.gitignore` - Node.js 前端特定
**作用**：处理 Node.js/React 相关的文件和目录

**包含内容**：
- `node_modules/` - Node 依赖
- `dist/`, `build/` - 构建产物
- `.vite/` - Vite 缓存
- `*.log` - 前端日志
- `.env.local` - 前端环境变量

### `.cursorignore` - Cursor 上下文忽略
**作用**：告诉 Cursor AI 哪些文件不要索引（节省上下文）

**包含内容**：
- `.temp/` - 临时文件
- `node_modules/` - 依赖（太大）
- `.venv/`, `venv/` - 虚拟环境
- `backend/logs/` - 日志
- `backend/data/` - 数据库
- `dist/`, `build/` - 构建产物

## ⚠️ 重要规则

### ✅ 必须遵守

1. **虚拟环境位置**
   - ✅ 正确：`backend/.venv/`
   - ❌ 错误：根目录的 `venv/` 或 `.venv/`
   - **原因**：虚拟环境属于后端，应该在后端目录内

2. **依赖管理**
   - ✅ 后端依赖：`backend/requirements.txt`
   - ✅ 前端依赖：`frontend/package.json`
   - ❌ 根目录不应该有 `package.json`（除非是 monorepo）

3. **日志位置**
   - ✅ 后端日志：`backend/logs/`
   - ✅ 前端日志：`frontend/logs/`（如果有）
   - ✅ 项目日志：`logs/`（根目录，项目级别）

4. **数据文件**
   - ✅ 后端数据：`backend/data/`
   - ❌ 根目录不应该有 `data/`

5. **临时文件**
   - ✅ 所有临时文件：`.temp/`
   - ✅ 被 `.gitignore` 和 `.cursorignore` 忽略

### ❌ 不应该存在的结构

**根目录不应该有：**
- ❌ `node_modules/` - 应该只在 `frontend/`
- ❌ `venv/` 或 `.venv/` - 应该只在 `backend/`
- ❌ `app/` - 已移到 `backend/app/`
- ❌ `data/` - 已移到 `backend/data/`
- ❌ `package.json` - 应该只在 `frontend/`（除非是 monorepo）

**如果发现这些，说明结构有问题，需要清理！**

## 📏 目录职责

### `backend/` - 后端服务
- **作用**: FastAPI 应用，提供 RESTful API
- **语言**: Python 3.8+
- **框架**: FastAPI + LangChain + LangGraph
- **虚拟环境**: `backend/.venv/` ⚠️ **必须在这里**
- **数据**: SQLite 数据库在 `backend/data/`
- **日志**: 在 `backend/logs/`
- **依赖**: `backend/requirements.txt`

### `frontend/` - 前端应用
- **作用**: Web 用户界面
- **语言**: TypeScript
- **框架**: React 18 + Vite
- **状态**: Zustand
- **依赖**: `frontend/node_modules/`
- **构建**: `frontend/dist/`

### `docs/` - 文档
- **作用**: 项目文档、部署指南等
- **格式**: Markdown

### `.temp/` - 临时文件 🆕
- **作用**: 存放临时文档、调试文件、草稿
- **特点**: 
  - 被 `.gitignore` 忽略（不会提交）
  - 被 `.cursorignore` 忽略（不占用上下文）
  - 可以随时删除

## 🧹 清理命令

### 清理根目录的错误结构
```bash
# 删除根目录的 venv（应该在 backend/）
rm -rf venv/

# 删除根目录的 node_modules（应该在 frontend/）
rm -rf node_modules/

# 删除根目录的 app/（应该在 backend/）
rm -rf app/
```

### 清理临时文件
```bash
rm -rf .temp/
```

### 清理构建产物
```bash
rm -rf frontend/dist/
rm -rf frontend/.vite/
rm -rf backend/dist/
```

### 清理依赖（重新安装时）
```bash
rm -rf backend/.venv/
rm -rf frontend/node_modules/
```

## 🔍 检查项目结构

### 检查根目录是否有错误文件
```bash
# 检查根目录是否有不应该存在的目录
ls -la | grep -E "venv|node_modules|app/|data/"

# 应该只看到 backend/ 和 frontend/ 目录
```

### 查看整体结构（不包括依赖）
```bash
tree -L 2 -I 'node_modules|.venv|__pycache__|.temp|.git' 

# 或
find . -maxdepth 2 -type d | grep -v -E 'node_modules|\.venv|__pycache__|\.temp|\.git' | sort
```

### 检查虚拟环境位置
```bash
# 应该只在 backend/ 有 .venv
find . -name ".venv" -o -name "venv" | grep -v ".temp"
# 应该只输出: ./backend/.venv
```

## 📊 大小统计

```bash
# 查看各目录大小
du -sh backend/ frontend/ docs/ .temp/

# 查看依赖大小
du -sh backend/.venv/ frontend/node_modules/
```

## 🤖 AI 助手记忆

**重要**：本项目结构遵循以下规则，AI 助手应该记住：

1. **虚拟环境位置**：`.venv/` 必须在 `backend/` 内，根目录不应该有
2. **忽略规则分层**：
   - 根目录 `.gitignore` = 项目级别
   - `backend/.gitignore` = Python 特定
   - `frontend/.gitignore` = Node.js 特定
3. **临时文件**：所有临时文件放在 `.temp/`，被双重忽略
4. **目录边界**：前后端完全分离，各自独立管理依赖和配置

**当创建新文件时，请遵循这些规则！**

---

**项目结构清晰，便于维护和扩展！** ✨
